<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.androidphotos">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".AndroidPhotos"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Androidphotos">

        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SearchActivity"></activity>
        <activity android:name=".CreateRenameAlbumActivity"></activity>
        <activity android:name=".EditTagsActivity"></activity>
        <activity android:name=".AddPhotoActivity"></activity>
        <activity android:name=".ViewAlbumActivity"></activity>
        <activity android:name=".DisplayPhotoActivity"></activity>
        <activity android:name=".SlideshowActivity"></activity>
    </application>

</manifest>